import React from "react";

const Hero = () => (
    <section className="hero">
        <h1>Welcome to Our Landing Page</h1>
        <p>Optimized for Performance & Speed</p>
    </section>
);

export default Hero;
